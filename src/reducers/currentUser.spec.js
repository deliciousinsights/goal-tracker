import reducer, { logIn, logOut } from './currentUser'

describe('Current User reducer', () => {
  it.todo('should return its initial state')

  it.todo('should handle login')

  it.todo('should handle logout')
})
