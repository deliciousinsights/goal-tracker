import { createReducer } from '@reduxjs/toolkit'

// Action Creators
// ---------------

export const logIn = 'FIXME'
export const logOut = 'FIXME'

// Réducteur
// ---------

export default createReducer(null, (builder) => {
  // FIXME
})
